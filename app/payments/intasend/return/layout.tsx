export const metadata = {
  title: "Payment | zimji",
  description: "IntaSend checkout return",
};

export default function IntasendReturnLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
